#ifndef LOG_STRUCT_H
#define LOG_STRUCT_H
#include <Arduino.h>
// struct logentry_t
// {
//   uint32_t t;
//   float u;
//   int32_t cnt;
//   uint8_t ctrl;
//   float y;
// }__attribute__((aligned(4)));
// #define MYDATA_FIELDS(F)       \
//     F(float,    y)             \
//     F(float,    u)             \
//     F(uint32_t, t)             \
//     F(int32_t,  cnt)           \
//     F(uint8_t,  ctrl)
// struct logentry_t
// {
//     // We define a temporary macro that transforms (type, name)
//     // into "type name;". Then we call MYDATA_FIELDS with that macro.
// #define DEFINE_FIELD(type, name) type name;

//     MYDATA_FIELDS(DEFINE_FIELD)

// #undef DEFINE_FIELD
// };




#endif